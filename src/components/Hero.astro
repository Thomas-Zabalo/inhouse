---
interface Props {
	title: string;
	description: string;
	promote?: boolean;
}
const { title, description, promote } = Astro.props;
---

<div class="relative isolate px-6 lg:px-8">
	<div class="mx-auto max-w-2xl py-32 sm:py-48 lg:py-56">
		{
			promote && (
				<div class="hidden sm:mb-8 sm:flex sm:justify-center">
					<div class="relative rounded-full px-3 py-1 text-sm/6 text-gray-400 ring-1 ring-white/10 hover:ring-white/20">
						Prêt pour le prochain round ?{" "}
						<a
							href="https://discord.gg/uUChN64H"
							class="font-semibold text-red-400"
						>
							<span aria-hidden="true" class="absolute inset-0" />
							Rejoins l'inhouse{" "}
							<span aria-hidden="true">&rarr;</span>
						</a>
					</div>
				</div>
			)
		}

		<div class="text-center">
			<h1
				class="text-3xl font-semibold tracking-tight text-balance text-white sm:text-7xl"
			>
				{title}
			</h1>
			<p
				class="mt-8 text-lg font-medium text-pretty text-gray-400 sm:text-xl/8"
			>
				{description}
			</p>
		</div>
	</div>
</div>
